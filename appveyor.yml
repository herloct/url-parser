version: '1.0.0-beta3.{build}'

clone_folder: C:\Users\appveyor\go\src\github.com\herloct\url-parser

before_build:
  - go env
  - go test -v

build_script:
  - go build

test_script:
  - ps: |
      url-parser --help
      if ($LastExitCode -eq 2) { $host.SetShouldExit(0) } else { $host.SetShouldExit($LastExitCode) }
